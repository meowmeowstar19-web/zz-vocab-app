# 这个项目的规矩（改 UI 前先看）

## 弹窗（pop）

- **最短也是正方形。** 单词本的单词 pop（`WordListPage.jsx` 的 `PopupDetail`）宽高
  下限是同一个值：内容再短也不许把卡压扁成一条，短内容是在方框里居中，不是让方框
  缩水。内容长了可以往下长，但绝不能比正方形矮。（用户反复强调过的死规矩）
- **一个 pop 只有一个退出控件。** 决策类 pop 用底部一对按钮（取消 + 确认），
  表单/内容类 pop 用右上角 X 或底部一个 CTA —— 详见 `general-ui/popKit.jsx` 顶部
  的说明。单词 pop 已定稿为「底部黄色关闭」，不要再加回右上角 X。
- 排版：一摞内容（图 / 单词 / 音标 / **释义** → 例句 / 译文 → 关闭按钮）整体在方卡里
  垂直居中。内容短不会挤在卡顶、中间也不留洞；内容长照旧从上往下排，超出屏幕卡自己滚。
- **释义（单词的翻译）永远紧跟着单词**，不许扔到下面跟例句一块儿排/一块儿居中。
- **关闭按钮永远离内容一个固定间距（16），不许钉在卡底边上** —— 贴底看着太靠下。

## 音标 / 读音

- **只显示数据里标注过的**（Excel 的 `ipa` 列、`phonetics.js` 的 `phoneticMap`、
  日文 `jaReading`、中文 `pinyin`），统一走 `langHelpers.getPhonetic`。
- 没标注的（进阶短语等）就**留空不显示**。不许再拿第三方词典 API 兜底按空格把词组
  拆开逐词猜 —— 那会给词组配上其中某个词的音标（"Have a falling-out" → `/æɪ/`）。
  宁可不显示，也不显示错的。

## 数据文件

- `src/data/` 下带「AUTO-GENERATED」抬头的文件由 `scripts/sync-data.mjs` 从 Excel
  生成，不要手改；要改内容改 Excel 再 `npm run sync`。
