.vercel
node_modules
dist

# Local secrets
.env
.env.local
.env.*.local

# macOS
.DS_Store

# Excel lock files (LibreOffice/Excel creates these when the file is open)
.~lock.*#
~$*

# Images of words deleted from WordList.xlsx (kept locally for recovery, not a published final)
_deleted-images/

# Retired: update_data_folder/ is gone — word-data/ holds the canonical xlsx now.
# (Big marketing video pipeline lives outside the app; never commit it.)
social-video/

# Local scratch / work-in-progress files (not used by the app)
confirmed_images/
Tab2_classified.csv
Tab2_进阶地道_语义分类.csv

# Claude Code worktrees (git-internal, not project code)
.claude/worktrees/

# Claude Code session lock
.claude/scheduled_tasks.lock

# Playwright cache (tests live outside the repo at ~/Desktop/data_prep/moneky-test/)
playwright/.cache/

# R2 upload script's local diff manifest (if introduced later for faster diffs)
scripts/.r2-upload-manifest.json

# Supabase CLI local link cache (project ref / pooler info — not project code)
supabase/.temp/

# 临时工作区(水印 venv/演示产物等),勿提交
tmp/
