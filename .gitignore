.vercel
node_modules
dist

# Local secrets
.env
.env.local
.env.*.local

# macOS
.DS_Store

# Excel lock files (LibreOffice/Excel creates these when the file is open)
.~lock.*#
~$*

# Archived originals from sync-data.mjs (we keep the compressed versions in public/images/)
update_data_folder/_processed_images/

# Images of words deleted from WordList.xlsx (kept locally for recovery)
update_data_folder/deleted_image/

# Source audio files (we keep the compressed versions in public/assets/audio/)
update_data_folder/audio/

# Drop zone for new audio recordings; consumed by scripts/sync-updated-audio.mjs
update_data_folder/updated_audio/

# Canonical uncompressed audio archive (compressed copies live in public/assets/audio/)
audio-未压缩-原版/

# Local scratch / work-in-progress files (not used by the app)
confirmed_images/
Tab2_classified.csv
Tab2_进阶地道_语义分类.csv

# Claude Code worktrees (git-internal, not project code)
.claude/worktrees/

# Claude Code session lock
.claude/scheduled_tasks.lock
